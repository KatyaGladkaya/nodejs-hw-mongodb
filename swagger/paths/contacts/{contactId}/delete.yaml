tags:
  - Контакти
summary: Видалити контакт
operationId: deleteContact
description: Видаляє контакт із бази даних.
security:
  - bearerAuth: []
parameters:
  - name: contactId
    in: path
    required: true
    schema:
      type: string
    description: Ідентифікатор контакту
responses:
  '200':
    description: Контакт успішно видалено
    content:
      application/json:
        schema:
          type: object
          properties:
            status:
              type: integer
              example: success
            message:
              type: string
              example: Contact deleted successfully
            data:
              type: object
              example: {}
  '400':
    description: Контакт не видалено
    content:
      application/json:
        schema:
          type: object
          properties:
            status:
              type: integer
              example: 400
            message:
              type: string
              example: Something went wrong
